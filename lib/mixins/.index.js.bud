'use strict'

const { dir } = require('the-templates')

module.exports = dir({
  name: 'mixins',
  dirname: __dirname,
  description: 'Mixin functions',
  node: true
})

if (!module.parent) {
  require('coz').render(__filename)
}
